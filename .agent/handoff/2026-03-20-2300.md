# HANDOFF — 2026-03-20 (updated)

## 完了した作業

### テスト・リファクタ（Task 1〜9）
- 49 tests / 0 fail（api:21, crawler:15, db:13）
- type-check: 全6パッケージ PASS

### ドキュメント刷新
- README.md 全面刷新、apps/*/README.md 新規作成

### 3つのUIバグ修正
1. **クレカ引き落とし未表示** → `apps/web/src/app/page.tsx` に `incomeExpense.upcomingWithdrawals` 呼び出し追加（commit: e26e381）
2. **グラフ期間変更無効 + dataKey ミスマッチ** → `AssetHistoryChart` を Client Component 化、`useEffect` で再fetch、`totalJpy` に修正（commit: d27ac4a）
3. **取得単価すべて0** → MF が cellTexts[6] を空で返す → `(評価額 - 含み損益) / 数量` で逆算フォールバック（commit: 6f52693）
4. **brittle test 修正** → `>= 47` → `> 0`（commit: 直前）

### スキル・ポリシー整備
- `test-driven-development` スキル: Value Validity・Full-Stack Connectivity セクション追加
- `verification-before-completion` スキル: 3パターン追加
- `docs/superpowers/specs/test-policy.md` 新規作成
- `CLAUDE.md`: スキル自律改定ルール追加

### 追加バグ修正（このセッション）

5. **謎の項目 ‹[22] ‹[15]** → スクレイパーに `nameIsValid` + `safeCandidate` 二重ガード追加（sora）
6. **モンテカルロ初期資産** → API から総資産取得して自動セット（luna）
7. **外貨資産が円表示** → `getHoldings()` で `.select()` を明示的に書き直し currency を確実に返却（cleo, commit 997fb14）
8. **HKD currency 誤設定** → DB UPDATE で HKD に修正（mira）
9. **月配当精度** → 毎月分配型・年1回・四半期・半期の4パターン対応（iris, commit aad9e0b）
10. **クレカ全件取得** → 全クレカをループ取得に変更（echo, commit cddb79b）
11. **金融機関名表示** → assets テーブルに institutionName 追加、スクレイパー・API・Web に反映（echo）
12. **投信・年金・ポイント表示** → PENSION/POINT ダミーレコード自動生成、タブ表示対応（echo）

## 現在の状態

- PM2: api/mcp/web/worker online、discord stopped
- テスト: 49 pass / 0 fail
- ビルド: 全パッケージ PASS（997fb14 時点）
- **次回スクレイプ実行後に自動反映されるもの**: 金融機関名、全クレカ、年金・ポイントデータ

## 次にやること

- スクレイプ実行して画面で目視確認（金融機関名・全クレカ・外貨表示・月配当）
- グラフ期間変更が実際に動作するか確認（DB に90日分データ蓄積後）
- feature_backlog に積んだ機能群の実装
